[project]
name = "ux-swarm"
version = "0.1.0"
description = "Synthetic UX testing"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "click>=8.1.8",
    "requests>=2.32.5",
]
[project.scripts]
ux-swarm = "main:cli"     
swarm = "main:cli"     